# dataEngine — API/Color/Convert/RgbToHex
# Macro args: {type: string, target: string, nbt: string}
$function data_api:command/modify_data/color/rgb_to_hex \
    {type: "$(type)", target: "$(target)", nbt: "$(nbt)"}
