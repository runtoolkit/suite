# dataEngine — API/Color/Convert/RgbToDec
# Macro args: {type: string, target: string, nbt: string}
# {r,g,b} compound'unu decimal'e dönüştürür.
$function data_api:command/modify_data/color/rgb_to_dec \
    {type: "$(type)", target: "$(target)", nbt: "$(nbt)"}
