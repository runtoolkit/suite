# dataEngine — API/Item/ToSlot
# data_api:private export.from = item compound olmalı.
# Macro args: {type: string, target: string, slot: string}
$function data_api:command/modify_data/export/to_slot \
    {type: "$(type)", target: "$(target)", slot: "$(slot)"}
