# dataEngine — API/Array/Shuffle
# Macro args: {type: string, target: string, nbt: string}
$function data_api:command/modify_data/array/shuffle_order \
    {type: "$(type)", target: "$(target)", nbt: "$(nbt)"}
