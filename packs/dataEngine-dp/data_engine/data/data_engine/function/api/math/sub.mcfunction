# dataEngine — API/Math/Sub
# Macro args: {type: string, target: string, nbt: string, value: int}
$function data_api:command/modify_data/number/subtract \
    {type: "$(type)", target: "$(target)", nbt: "$(nbt)", value: $(value)}
