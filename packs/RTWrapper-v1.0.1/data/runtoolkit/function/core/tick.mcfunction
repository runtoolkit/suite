# Runtoolkit shared registry tick hook. Currently a no-op: the loaded-pack registry is
# advancement-based (see core/load.mcfunction) and does not need per-tick work. Kept as a
# tag target so other Runtoolkit datapacks can extend #runtoolkit tick behavior later
# without needing to touch minecraft:tick_functions.
