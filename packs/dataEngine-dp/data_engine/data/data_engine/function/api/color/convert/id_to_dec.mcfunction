# dataEngine — API/Color/Convert/IdToDec
# Macro args: {type: string, target: string, nbt: string}
# Minecraft renk id'sini decimal'e dönüştürür.
$function data_api:command/modify_data/color/id_to_dec \
    {type: "$(type)", target: "$(target)", nbt: "$(nbt)"}
