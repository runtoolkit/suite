# dataEngine — API/String/ArrayToString
# Macro args: {type: string, target: string, nbt: string}
# Karakter dizisini string'e birleştirir.
$function data_api:command/modify_data/array/to_string \
    {type: "$(type)", target: "$(target)", nbt: "$(nbt)"}
