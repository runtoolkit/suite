# dataEngine — API/Color/Convert/DecToHex
# Macro args: {type: string, target: string, nbt: string}
# Decimal renk değerini hex string'e dönüştürür.
$function data_api:command/modify_data/color/dec_to_hex \
    {type: "$(type)", target: "$(target)", nbt: "$(nbt)"}
