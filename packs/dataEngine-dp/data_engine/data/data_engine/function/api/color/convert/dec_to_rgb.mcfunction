# dataEngine — API/Color/Convert/DecToRgb
# Macro args: {type: string, target: string, nbt: string}
# Decimal renk değerini {r,g,b} compound'a dönüştürür.
$function data_api:command/modify_data/color/dec_to_rgb \
    {type: "$(type)", target: "$(target)", nbt: "$(nbt)"}
