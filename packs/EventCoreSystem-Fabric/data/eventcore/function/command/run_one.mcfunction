# Tek bir ham komutu çalıştırır.
# Beklenen macro alanı: {cmd:"komut metni"}
# NOT: Bu fonksiyon command/run'daki güvenlik filtrelerini atlar.
# Güvenlik gerektiren kullanımlar için type:"cmd" tercih edilmeli.
$$(cmd)
