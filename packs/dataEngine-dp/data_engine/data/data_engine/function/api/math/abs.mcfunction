# dataEngine — API/Math/Abs
# Macro args: {type: string, target: string, nbt: string}
$function data_api:command/modify_data/number/absolute_value \
    {type: "$(type)", target: "$(target)", nbt: "$(nbt)"}
