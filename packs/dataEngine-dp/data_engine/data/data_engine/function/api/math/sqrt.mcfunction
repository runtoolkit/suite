# dataEngine — API/Math/Sqrt
# Macro args: {type: string, target: string, nbt: string}
$function data_api:command/modify_data/number/square_root \
    {type: "$(type)", target: "$(target)", nbt: "$(nbt)"}
