# dataEngine — Util/SetTargetSelectors
# data_engine:private selector_args storage'a koy (import/export compound formatında).
# data_api:command/set_target_selectors tam wrapper — selector string'leri çözümler.
function data_api:command/set_target_selectors \
    with storage data_engine:private selector_args
