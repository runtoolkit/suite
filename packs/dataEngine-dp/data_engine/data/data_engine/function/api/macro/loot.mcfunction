# dataEngine — API/Macro/Loot
# Macro args: {loot: string, type: string, target: string, slot: string}
# data_api:command/macro/loot tam wrapper — loot komutu üretir.
$function data_api:command/macro/loot \
    {loot: "$(loot)", type: "$(type)", target: "$(target)", slot: "$(slot)"}
