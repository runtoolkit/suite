# dataEngine — API/Block/Raycast
# @s olarak çalıştırılmalı.
# Macro args: {command: string, distance: string}
# Bakılan blok üzerinde komut çalıştırır.
$function data_api:command/raycast/on_block \
    {command: "$(command)", distance: "$(distance)"}
