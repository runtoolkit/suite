# dataEngine — API/Array/ToCompound
# Macro args: {type: string, target: string, nbt: string}
# Listeyi compound'a dönüştürür.
$function data_api:command/modify_data/array/to_compound \
    {type: "$(type)", target: "$(target)", nbt: "$(nbt)"}
