# dataEngine — API/Color/Convert/HexToRgb
# Macro args: {type: string, target: string, nbt: string}
$function data_api:command/modify_data/color/hex_to_rgb \
    {type: "$(type)", target: "$(target)", nbt: "$(nbt)"}
