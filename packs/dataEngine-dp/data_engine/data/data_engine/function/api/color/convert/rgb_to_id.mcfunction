# dataEngine — API/Color/Convert/RgbToId
# Macro args: {type: string, target: string, nbt: string}
$function data_api:command/modify_data/color/rgb_to_id \
    {type: "$(type)", target: "$(target)", nbt: "$(nbt)"}
