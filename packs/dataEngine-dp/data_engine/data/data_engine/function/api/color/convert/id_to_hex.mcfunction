# dataEngine — API/Color/Convert/IdToHex
# Macro args: {type: string, target: string, nbt: string}
$function data_api:command/modify_data/color/id_to_hex \
    {type: "$(type)", target: "$(target)", nbt: "$(nbt)"}
