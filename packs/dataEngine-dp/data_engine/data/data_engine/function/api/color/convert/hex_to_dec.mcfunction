# dataEngine — API/Color/Convert/HexToDec
# Macro args: {type: string, target: string, nbt: string}
$function data_api:command/modify_data/color/hex_to_dec \
    {type: "$(type)", target: "$(target)", nbt: "$(nbt)"}
