# dataEngine — API/Color/Convert/HexToId
# Macro args: {type: string, target: string, nbt: string}
$function data_api:command/modify_data/color/hex_to_id \
    {type: "$(type)", target: "$(target)", nbt: "$(nbt)"}
