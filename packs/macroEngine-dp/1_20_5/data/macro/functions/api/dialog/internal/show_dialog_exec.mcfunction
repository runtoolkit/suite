# macro:api/dialog/internal/show_dialog_exec [1.20.5 overlay]
# Stub — $dialog show requires 1.21.6+.
# In 1.21.6+ this fires: $dialog show $(NAME) $(DIALOG)
# On this version the command does not exist; stub returns silently.
return 0
